# mcp 2.0 renamed FastMCP to MCPServer and dropped mcp.server.fastmcp, which
# server.py imports. The 1.10 floor is where mcp.server.transport_security first
# appears (also imported by server.py); 1.8/1.9 resolve but fail at import.
mcp[cli]>=1.10,<2
uvicorn
# No boto3: the orders slice comes from SQLite, and there is no AWS in this build.
# Pinned to the control-plane version installed by scripts/install-agent-sandbox.sh
# (agent-sandbox v0.5.0). The SDK and the controller share a CRD contract, so they
# should move together.
k8s-agent-sandbox==0.5.0
